#!/usr/bin/env zsh

# LOG_FILE="/tmp/log/zsh_startup.log"
# echo "$(date '+%Y-%m-%d %H:%M:%S') - .config/zsh/.zshenv start executed" >> "$LOG_FILE"

# echo "Running ~/.config/zsh/.zshenv"
# echo "Running $0"
source ~/.common_environment

# These are in above file.
#
# export NVM_DIR="$HOME/.nvm"
#
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
#
# echo "$(date '+%Y-%m-%d %H:%M:%S') - .config/zsh/.zshenv end executed" >> "$LOG_FILE"
